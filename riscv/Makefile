# Simple Makefile to allow build hooks; the OJ will use its own Makefile.
# Provide a dummy target to build with iverilog if available, otherwise no-op.

all:
	@echo "Local build not implemented; relying on OJ testbench."

clean:
	@echo "Clean"
